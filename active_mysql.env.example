# Which MySQL host the backend uses for DB_HOST (writes + reads).
# Composer menu "Set active MySQL write node" updates this file.
# After changing, run "Apply DB_HOST to Rishu backend" or restart backend manually.

DB_WRITE_HOST=10.67.151.166
